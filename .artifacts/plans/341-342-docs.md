# Plan: Add CLAUDE.md and ONBOARDING.md (#341, #342)

**Objective:** Create CLAUDE.md and ONBOARDING.md for the aiConstitution repo so that AI assistants and new contributors understand the two-repo model and can build/test/contribute without guessing.

## Alternatives Table

| Alternative | Pros | Cons | Verdict |
|---|---|---|---|
| Add both files in a single commit | Fewer commits | Mixes two distinct concerns (AI context vs. human onboarding) | Rejected |
| Two files, two commits (chosen) | Keeps Git Commit Isolation; each file is independently reviewable | Slightly more ceremony | Chosen |
| Embed content in existing README.md | One file | README is already well-scoped; adding CLAUDE.md/ONBOARDING.md is additive, not redundant | Rejected |

## Scope

- Files to create: `CLAUDE.md`, `ONBOARDING.md` — both at worktree root
- Plan artifact: `.artifacts/plans/341-342-docs.md`
- No source files modified

## Approach

1. Read source: `README.md`, `go.work`, `Makefile`, `.github/workflows/ci.yml`, `src/cmd/ai/cmd/root.go`, `CONTRIBUTING.md`
2. Write `CLAUDE.md` from spec in issue #341
3. Write `ONBOARDING.md` derived from actual source content
4. Adversarial self-pass (below)
5. Commit each file separately
6. Push + PR

## Testing strategy

Docs-only change — TDD Writer skip per `Code.md §11.8`.

## Risk assessment

- Invented commands / wrong Go version → mitigated by reading actual source files
- User data paths mentioned → const check: no `~/.ai/` paths in CLAUDE.md except in the "data" column of the two-repo table and in the governance contract section (appropriate)

## Adversarial self-pass

CLAUDE.md:
- No user data committed (paths are descriptive only, not actual files in repo)
- No secrets referenced
- Two-repo description matches README.md exactly

ONBOARDING.md:
- Go version: `go.work` says `go 1.26.3`, CI uses `"1.26"` — stated as Go 1.26+
- `make build` → verified in Makefile (produces `dist/ai`)
- `make test` → verified in Makefile
- `make lint` → verified in Makefile
- CLI commands come from `root.go` `AddCommand` list — verified, not invented
- Directory structure from README.md and actual `ls` output — verified

## Dependencies

None.

## Backward compatibility

Additive only — no existing files modified.
